<UserControl x:Class="CampingApplication.VisitorApp.Views.Map.FacilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CampingApplication.VisitorApp.Views.Map"
             xmlns:viewmodels="clr-namespace:CampingApplication.VisitorApp.ViewModels"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d"
             Width="35" Height="35"
             d:DataContext="{d:DesignInstance Type=viewmodels:FacilityViewModel}"             >
    <!-- Border for the Facility -->
    <Border CornerRadius="100" Background="{DynamicResource Primary}" Padding="8">
        <ContentControl Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock FontWeight="ExtraBold" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Text="?" />
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <!-- Default icon -->

                    <Style.Triggers>
                        <!-- Trigger for type 'Restroom' -->
                        <DataTrigger Binding="{Binding Type}" Value="Restroom">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <svg:SvgViewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Source="pack://application:,,,/Resources/Icons/restroom.svg" />
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <!-- Trigger for type 'Shower' -->
                        <DataTrigger Binding="{Binding Type}" Value="Shower">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <ContentControl Foreground="White">
                                            <svg:SvgViewbox MessageFillBrush="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Source="pack://application:,,,/Resources/Icons/shower.svg" />
                                        </ContentControl>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <!-- Trigger for type 'Playground' -->
                        <DataTrigger Binding="{Binding Type}" Value="Playground">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <svg:SvgViewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Source="pack://application:,,,/Resources/Icons/playground.svg" />
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Type}" Value="Reception">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <TextBlock FontWeight="ExtraBold" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Text="R" />
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Border>
</UserControl>
