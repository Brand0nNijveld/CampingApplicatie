<UserControl x:Class="datapicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:datapicker"
        mc:Ignorable="d"
        >

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Begin DatePicker -->
        <StackPanel Orientation="Horizontal" Margin="0 10 0 10">
            <TextBlock Text="Begindatum:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <DatePicker x:Name="BeginDatePicker" SelectedDateChanged="BeginDatePicker_SelectedDate"/>
        </StackPanel>

        <!-- End DatePicker -->
        <StackPanel Orientation="Horizontal" Margin="0 10 0 10" Grid.Row="1">
            <TextBlock Text="Einddatum:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <DatePicker x:Name="EndDatePicker" SelectedDateChanged="EndDatePicker_SelectedDate"/>
        </StackPanel>

        <!-- Result TextBlock -->
        <TextBlock x:Name="ResultTextBlock" Grid.Row="2" FontSize="14" Foreground="Black" HorizontalAlignment="Left"/>

    </Grid>
</UserControl>
